{
  "name": "alertasimica",
  "version": "1.0.0",
  "private": true,
  "description": "AlertaSísmica — Plataforma de alertas sísmicas para Colombia (PWA + Backend)",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend",
    "test:coverage": "npm run test:coverage --prefix backend",
    "seed": "npm run seed --prefix backend",
    "vapid:generate": "npm run vapid:generate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
